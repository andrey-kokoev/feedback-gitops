{
  "name": "feedback-gitops",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @feedback-gitops/worker run dev",
    "deploy": "pnpm --filter @feedback-gitops/worker run deploy",
    "typecheck": "pnpm --filter @feedback-gitops/worker run typecheck",
    "provision:instance": "node scripts/provision-instance.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "playwright": "^1.58.2"
  }
}
